/* import { render } from "@testing-library/react"
import Navlinks from './Navlinks';

jest.mock('./Navlinks', () => {
    return {
        link: jest.fn(() => {
            return {

            }

        })
    }
}) */